<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Ma démo" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Demo" %>">
    
    <script src="//ajax.googleapis.com/ajax/libs/angularjs/1.2.3/angular.js"></script>

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <%= yield :stylesheets %>


  </head>
  <body ng-app="demoApp">
     
    <main role="main" ng-controller="mainCtrl">
       <%= render 'layouts/messages' %>
       
       <div class="container">
         <%= yield %>
       </div>

    </main>
  </body>
</html>
